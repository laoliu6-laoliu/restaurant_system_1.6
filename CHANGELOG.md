# 版本记录

## v1.6 / 1.6.0 — 2026-09-11

顾客端新增独立的“我的预约”页面。当前预约以餐桌信息卡展示，包含餐桌编号、座位数、实时桌况、预约起止时间、到店截止时间、用餐人数、联系电话、预约来源、预约编号、押金金额与状态；已完成、已取消和未到店记录归入历史预约。

预约页面沿用现有账号隔离，后端只返回当前顾客自己的预约；餐桌状态与预约资料支持手动刷新并每 5 秒自动更新。v1.6 不改变数据库结构。验证覆盖 17 项后端集成测试、15 项前端浏览器测试、前端生产构建及真实运行接口检查。

## v1.5 / 1.5.0 — 2026-09-10

订单新增独立的生命周期状态和结束时间。员工与管理员均可结束进行中的订单；订单结束后餐桌转为待清理，换桌和继续支付会被服务端拒绝，员工端与管理员端同步隐藏不适用的操作。

删除规则改为只允许删除已结束订单。删除已结束的待支付订单会恢复菜品库存；删除已支付订单会删除对应支付记录和菜品明细，但不会回补已售库存。数据库启动升级会自动增加 `orders.order_status` 与 `orders.ended_at` 字段。验证覆盖 17 项后端集成测试、15 项前端浏览器测试、前端生产构建及真实运行接口检查。

## v1.4 / 1.4.0 — 2026-09-10

员工订单管理允许待支付和已支付订单办理换桌。已支付订单在列表中继续显示“换桌”入口，换桌表单会标明当前支付状态；服务端不再因订单已支付而拒绝换桌。

换桌仍要求实际人数超过原桌座位数，并只允许锁定座位足够的空闲大桌。换桌只更新订单餐桌和现场餐桌状态，已支付订单的金额与支付状态保持不变。v1.4 不改变数据库结构；验证覆盖 17 项后端集成测试、14 项前端浏览器测试、前端生产构建及真实运行接口检查。

## v1.3 / 1.3.0 — 2026-09-10

员工餐桌管理改为按当天预约显示状态。只有未来日期存在预约、今天没有预约的餐桌显示为空闲；今天有有效预约的餐桌显示“已被预订”，并列出每条预约的起止时间。

使用中和待清理继续作为现场状态显示，同时保留当天预约时间提示。后端在餐桌查询时按上海时区自动同步今天的预约状态，跨日后无需员工手动清除昨天或未来预约造成的“已预订”。

v1.3 不改变数据库结构。验证覆盖 17 项后端集成测试、14 项前端浏览器测试、前端生产构建及真实运行接口检查。

## v1.2 / 1.2.0 — 2026-09-10

预约占用规则改为按餐桌和起止时间判断。同一餐桌可以保存不同日期或同日不重叠时段的多条预约；重复和交叉时段由带餐桌锁的服务端校验拒绝，避免并发重复预约。

顾客餐桌状态页允许从所有实时状态的餐桌查看未来可约时段。已占用时段会显示“已被预约”并禁止选择，同时新增电话咨询提醒，建议顾客预约前致电餐厅确认人数和时段。员工电话预约也不再因该桌存在其他日期预约而被隐藏。

取消或扣除其中一条预约押金时，系统会检查该桌是否还有其他有效预约，避免错误释放餐桌。v1.2 不改变数据库结构；验证覆盖 17 项后端集成测试、13 项前端浏览器测试、前端生产构建及真实运行接口检查。

## v1.1 / 1.1.0 — 2026-09-10

员工预约管理界面新增明确的“取消订单且不退回押金”操作。顾客在预约开始后仍未到店时，员工确认操作即可取消订单、扣除已支付押金并释放餐桌。

未到店预约状态统一显示为“订单已取消（押金不退）”，确认框会展示不予退还的押金金额；普通取消入口改为“正常取消并退押金”，减少误操作。

v1.1 不改变数据库结构。验证覆盖 16 项后端集成测试、13 项前端浏览器测试、前端生产构建及真实运行接口检查。

## v1.0 / 1.0.0 — 2026-09-10

完善顾客未到店处理。预约超过到店截止时间后自动变为“未到店已取消”，押金标记为已扣除且不退款，预约餐桌立即恢复空闲。

员工可在预约时间开始后通过预约管理手动确认顾客未到店。服务端限制操作权限与预约时间，防止顾客自行扣除押金或在预约尚未开始时误操作；正常取消继续退还押金。

v1.0 不改变数据库结构。验证覆盖 16 项后端集成测试、13 项前端浏览器测试、前端生产构建及真实运行接口检查。

## v0.9 / 0.9.0 — 2026-09-10

员工端新增统一预约管理视图，顾客线上完成押金支付后的预约会与员工电话预约一起显示。预约记录新增来源字段，列表明确展示线上预约或电话预约、预约时间、餐桌编号、顾客信息、人数、押金及状态。

管理员订单明细查询改为按订单分页，每个订单只显示一行订单编号。管理员点击“订单详情”后再加载并查看该订单所点菜品、数量、金额和备注，避免同一订单按菜品拆成多行。

新增 v0.9 数据库升级并补齐历史预约来源。验证覆盖 15 项后端集成测试、12 项前端浏览器测试、前端生产构建及真实 MySQL 增量升级。

## v0.8 / 0.8.0 — 2026-09-10

餐桌增加至 15 张：2、4、6、8、12 人桌各 3 张。新增 12 人桌及 ¥70 预约押金展示，原有预约、超时扣除、换桌和结账退款流程继续适用于新桌型。

菜品由 12 道增加至 24 道，新增主菜、主食、汤品、凉菜、小吃、甜品和饮品共 12 道，并为所有新增菜品生成独立的本地图片。新增 v0.8 可重复执行的数据库升级，只补充缺失记录并保留已有业务数据与自定义菜单内容。

验证覆盖 15 项后端集成测试、10 项前端浏览器测试、前端生产构建、真实 MySQL 增量升级及 24 张图片加载检查。

## v0.7 / 0.7.0 — 2026-09-09

顾客端餐桌状态页升级为实时预约入口，展示空闲、使用中、待清理和已预订四种状态并每 5 秒刷新。顾客可选择空闲桌、日期、固定 2 小时时段和人数，系统按桌型计算押金；完成微信或支付宝模拟支付后立即锁定餐桌。

预约开始后设置 30 分钟到店宽限。员工按时接待并开单后，账单支付沿用原有押金退款流程；超过截止时间仍未接待的预约由后台定时任务标记为未到店，押金标记为扣除并释放餐桌。顾客只能读取自己的预约资料，姓名和电话由登录账号提供。

新增 v0.7 数据库升级，保存时段结束、到店截止和押金扣除时间。验证覆盖 15 项后端集成测试、9 项前端浏览器测试、前端生产构建与页面视觉检查。

## v0.6 / 0.6.0 — 2026-09-09

员工订单管理新增换桌流程。员工按订单日期和当天 `001`–`999` 编号定位未支付订单，登记实际到店人数后，可将订单换到能够容纳全部顾客的更大空闲餐桌。

服务端以事务和数据库行锁同时更新订单及餐桌：新桌锁定为“使用中”，原桌转为“待清理”。已支付订单、人数未超过原桌容量、新桌容量不足或新桌已占用时拒绝操作；预约订单换桌会同步预约绑定，保留押金退款关系。

v0.6 不改变数据库结构。验证覆盖 14 项后端集成测试、8 项前端浏览器测试和前端生产构建。

## v0.5 / 0.5.0 — 2026-09-09

新增员工电话预约工作台，可登记预约人姓名、联系电话、人数、预约时间和餐桌，并在收取模拟押金后自动将餐桌锁定为“已预订”。押金按餐桌容量计费：2 人桌 ¥20，之后每增加 2 个座位增加 ¥10。

员工可从预约记录直接接待并创建订单。预约与订单绑定后，顾客或员工完成账单支付时，系统自动将押金标记为已退回并在支付回执中显示；取消未入座预约也会退押金并释放餐桌。餐桌不能绕过预约流程直接改为“已预订”。

新增预约表及 v0.5 自动升级。验证：13 项后端集成测试、7 项前端浏览器测试、前端生产构建、本机 MySQL 预约迁移与取消清理，以及隔离数据库中的完整押金退款流程。

## v0.4 / 0.4.0 — 2026-09-09

管理员运营仪表盘新增月份选择器。总营收按所选月份内成功支付的流水统计，当月订单数按下单时间统计；菜品销量、销售额、Top 5 和销量冠军均按同一个所选月份计算，仅纳入当月成功支付的订单。

管理员销售排行页共用同一月份选择，并显示该月完整排行。员工端与管理员端移除支付记录页面，订单收款功能继续保留。

验证：12 项后端集成测试、6 项前端浏览器测试、前端生产构建，以及本机 MySQL 月度报表接口检查。

## v0.3 / 0.3.0 — 2026-09-09

修改密码时必须输入旧密码，并输入两次一致的 6–20 位新密码；普通资料修改接口不再接受密码字段。顾客、员工和管理员都使用独立的修改密码窗口。

新增按营业日生成的三位订单编号，每天从 `001` 重新开始，到 `999` 为止。数据库仍保留内部订单主键用于关联，界面和支付流水显示“订单日期 + 当日编号”。历史订单在升级时按原下单时间自动补号。

订单、订单明细视图和支付记录默认只查询当天，支持选择开始与结束日期、分页和每页数量。订单明细在展开某个订单时按需加载，不再一次读取全部历史记录。

验证：11 项后端集成测试、6 项前端浏览器测试、前端生产构建，以及本机 MySQL 增量升级和运行接口检查。

## v0.2 / 0.2.0 — 2026-09-08

新增模拟支付、支付流水、顾客支付窗口、员工收款与管理员支付记录；将下单改为后端事务，增加库存检查和重复支付保护。新增 6 道菜，补齐 12 张本地菜品图片，提供图片地址编辑及移动端展示。后端改用 8082、前端改用 5174。数据库自动增量升级，保留已有业务数据。

支付相关订单与明细接口需要登录；`POST /api/orders` 与 `/api/orders/checkout` 均接收 `tno`、`items`，员工代客下单另传 `cno`，可选 `ono`。金额与员工身份由服务端确定。旧版直接修改订单 / 明细接口已关闭，防止修改已付款账单。支付写入接口为 `POST /api/payments/{ono}`，请求体中的 `method` 仅支持 `DEMO_WECHAT`、`DEMO_ALIPAY`、`DEMO_CASH`。

验证：9 项后端集成测试、4 项前端浏览器测试、生产构建，以及本机 MySQL 登录和 12 张菜品图片加载检查。

## v0.1 — 原始版本

以 Git 标签 v0.1 保留开始修改前的项目源码，包括顾客、员工、管理员界面及原有数据库脚本。
