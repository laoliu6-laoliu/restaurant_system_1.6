# 视图信息

## （1）视图 1：订单消费明细视图

① 名称：订单消费明细（订单号，下单时间，顾客编号，顾客姓名，员工工号，员工姓名，餐桌号，菜品编号，菜品名称，菜品类型，菜品数量，菜品单价，消费金额，备注）

② 作用：该视图将订单表、顾客表、员工表、餐桌表、订单细则表、菜品表六张表的核心字段整合为一张扁平化的订单消费明细表。用户查询订单明细时，只需按订单号、顾客编号或时间筛选即可获得完整的订单消费信息，无需编写复杂的多表连接语句。该视图不仅显著简化了前端应用的数据获取逻辑，也保证了查询结果的一致性。

### B. 视图信息

1. 订单消费明细（订单号，下单时间，顾客编号，顾客姓名，员工工号，员工姓名，餐桌号，菜品编号，菜品名称，菜品类型，菜品数量，菜品单价，消费金额，备注）

`v_order_detail_info(ono, order_time, cno, cname, eno, ename, tno, mno, mname, mtype, dish_count, dish_price, amount, remark)`

## （2）视图 2：菜品销售排行视图

① 名称：菜品销售排行（菜品编号，菜品名称，菜品类型，总销量，总销售额）

② 作用：该视图预先计算每道菜品的总销售数量和总销售金额，并按销量或销售额进行统计分析。管理员利用该视图可直接提取前 10 条记录，快速生成“热销菜品排行榜”；门店也可借助该视图统计各类菜品的销售表现，用于菜品优化和经营决策。该视图避免了每次排行榜请求时实时扫描订单细则表并重复计算所有菜品的销量和销售额，提升了查询响应速度。

2. 菜品销售排行（菜品编号，菜品名称，菜品类型，总销量，总销售额）

`v_dish_sales_rank(mno, mname, mtype, total_count, total_amount)`

# 各数据表索引设计

## 1. 管理员表 `administrator(ano, aname, apassword)`

主码：`ano`，系统自动创建主码唯一索引，满足按编号查询需求。

分析：`apassword`（密码）仅用于登录校验，查询频次低、存在更新操作，不额外建索引。

最终索引：仅保留系统自动生成的主码索引。

## 2. 员工表 `employee(eno, ename, eage, esex, epassword)`

主码：`eno`，系统自动创建主码唯一索引，支撑员工编号查询及多表连接。

业务场景：常按员工姓名（`ename`）查询员工信息，查询频次高、字段值相对稳定。

最终索引：为 `ename` 创建普通索引。

## 3. 顾客表 `customer(cno, cname, csex, cphone, cpassword)`

主码：`cno`，系统自动创建主码唯一索引，支撑顾客编号查询及多表连接。

业务场景：

- 常按顾客姓名（`cname`）查询顾客数据，查询频次高、字段值相对稳定。
- 常按顾客电话（`cphone`）查询顾客数据，查询频次高、字段值相对稳定。

最终索引：为 `cname`、`cphone` 创建普通索引。

## 4. 餐桌表 `dining_table(tno, seats, tstatus)`

主码：`tno`，系统自动创建主码唯一索引。

分析：当前业务中主要按餐桌号直接定位餐桌信息，座位数和状态字段数据规模较小，未额外建立索引。

最终索引：仅保留系统自动生成的主码索引。

## 5. 菜品表 `menu(mno, mname, mtype, mprice)`

主码：`mno`，系统自动创建主码唯一索引。

业务场景：

- 常按菜品名称（`mname`）检索菜品信息。
- 常按菜品类型（`mtype`）筛选菜品信息。

最终索引：为 `mname`、`mtype` 创建普通索引。

## 6. 订单表 `orders(ono, total_amount, order_time, eno, cno, tno)`

主码：`ono`，系统自动创建主码唯一索引。

外键：`eno`（关联员工表）、`cno`（关联顾客表）、`tno`（关联餐桌表），是多表关联核心字段，查询频繁。

业务场景：常按下单时间（`order_time`）筛选订单数据。

最终索引：为 `eno`、`cno`、`tno`、`order_time` 创建普通索引。

## 7. 订单细则表 `order_detail(dno, ono, mno, dish_count, amount, remark)`

主码：`dno`，系统自动创建主码唯一索引。

约束要求：`(ono, mno)` 为组合候选码，需保证同一订单下同一菜品组合唯一。

外键：`ono`、`mno` 为外键，是跨表连接、订单明细查询的核心字段。

业务场景：菜品编号 `mno` 常用于按菜品统计销量与消费数据。

最终索引：

- 为 `(ono, mno)` 创建联合唯一索引。
- 为 `mno` 创建普通索引。

# （二）索引设计汇总

1. `administrator`：主码 `ano` 自动生成主码索引，无额外索引。
2. `employee`：主码 `eno` 自动生成主码索引；新增普通索引：`ename`。
3. `customer`：主码 `cno` 自动生成主码索引；新增普通索引：`cname`、`cphone`。
4. `dining_table`：主码 `tno` 自动生成主码索引，无额外索引。
5. `menu`：主码 `mno` 自动生成主码索引；新增普通索引：`mname`、`mtype`。
6. `orders`：主码 `ono` 自动生成主码索引；新增普通索引：`eno`、`cno`、`tno`、`order_time`。
7. `order_detail`：主码 `dno` 自动生成主码索引；新增联合唯一索引：`(ono, mno)`；新增普通索引：`mno`。
